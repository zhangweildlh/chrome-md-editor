# Issue 验收报告 (#1 / #2 / #3)

Date: 2026-07-13  
Version under test: **1.3.0** (`main`)  
Gate command: `npm run qa:issues`

## Honesty first

我们欠反馈者一份**可复现的验收**，而不是口头「应该修好了」。

本报告区分：

| 层级 | 含义 |
|------|------|
| **A 自动化契约** | 单测 + 构建产物检查，CI 可反复跑 |
| **B 本机浏览器** | 必须在你的 Chrome 里点一次（扩展权限 / file URL 无法在无头浏览器里完整代替） |

本环境尝试了 Playwright 加载 MV3 扩展，**未能拿到 extensionId**（headless / 自动化环境限制）。因此 **B 层需要你本人 5 分钟** 才能闭环。

---

## Issue → 验收标准 → 结果

### #1 本地图 + 路径乱码（ShayneLu, 2026-03-31）

| # | 标准 | 层级 | 结果 |
|---|------|------|------|
| 1.1 | `file://` 上下文下相对路径图片可解析为 `file://` 绝对地址 | A | **PASS** |
| 1.2 | 「打开文件夹」目录上下文可解析 `images/...` | A | **PASS** |
| 1.3 | 无上下文时不假装能显示（返回 null，避免静默错误图） | A | **PASS** |
| 1.4 | 预览改写为 blob 后，回写源码优先 `data-md-original-src`，**禁止 blob 进 md** | A | **PASS** |
| 1.5 | 预览往返空行被规范化 | A | **PASS** |
| 1.6 | 真机：打开含 `images/` 的文件夹 → 预览可见图 | B | **待你点** |
| 1.7 | 真机：预览区改字再回编辑区 → 源码路径仍是相对路径 | B | **待你点** |

相关测试：`tests/issue-acceptance.test.js`（Issue #1 段）、`tests/image-support.test.js`

### #2 恢复上次文件（letomllcy, 2026-05-01）

| # | 标准 | 层级 | 结果 |
|---|------|------|------|
| 2.1 | lastFile 记录可序列化且校验可过期 | A | **PASS** |
| 2.2 | `chrome.storage` mock：remember → load → clear 闭环 | A | **PASS** |
| 2.3 | 真机：编辑后关标签，再点扩展图标，内容/文件名恢复 | B | **待你点** |
| 2.4 | 已知限制：FileHandle 无法持久化，恢复后保存可能要「另存为」 | 文档 | **已说明** |

### #3 多开 + 样式工具栏 + 帮助（zhangweildlh, 2026-05-18）

| # | 标准 | 层级 | 结果 |
|---|------|------|------|
| 3.1 | instanceId 批量唯一；pending 键不撞车 | A | **PASS** |
| 3.2 | background 源码含 `?i=` / `pendingFile_`，且不再单例 reuse query | A | **PASS** |
| 3.3 | 工具栏 DOM 含居中/高亮/上下标/字号 | A | **PASS** |
| 3.4 | mark/center/font/sup/sub 在 html→md 往返中保留 | A | **PASS** |
| 3.5 | onboarding 含 mark / 嵌套引用 / 上下标提示 | A | **PASS** |
| 3.6 | 真机：点两次扩展图标 = 两个编辑器标签 | B | **待你点** |
| 3.7 | 真机：工具栏插入后预览可见 | B | **待你点** |

---

## 自动化跑分（本机已执行）

```text
npm test          → 30/30 pass
npm run qa:issues → GATE PASSED (automated)
```

---

## 请你完成的 5 分钟 B 层清单

1. Chrome → `chrome://extensions` → 移除旧的 Markdown Editor  
2. **加载已解压** →  
   `/Users/mahaoxuan/Desktop/AI产品经理/实验探索/markdown 编辑器/dist`  
3. 打开 **允许访问文件网址**  
4. 用样例目录（若存在）`/tmp/md-editor-issue-fixture`：  
   - 「打开文件夹」选它 → 打开 `demo.md` → 看图是否出  
   - 在预览改一行字 → 回左边看 `images/red.png` 是否仍在  
5. 写几句字 → 关掉标签 → 再点扩展图标 → 是否提示「已恢复」  
6. 连点两次扩展图标 → 是否两个标签  

任一项失败：截图 + 步骤发我，当作回归，不当作「用户不会用」。

---

## 结论（对反馈者是否诚实）

| Issue | 自动化是否支撑「已解决」 | 能否对用户说 100% 修完 |
|-------|--------------------------|------------------------|
| #1 | 核心路径逻辑 **是** | 差真机图预览最后一跳 |
| #2 | 存储闭环 **是** | 差真机开关标签一次 |
| #3 | 多实例/工具栏契约 **是** | 差真机点两下图标 |

**在你完成 B 层之前，我们对外可以说：逻辑与回归测试已通过，真机请按上表验收。**  
B 层全绿后，才可以说：他们提的问题我们从端到端确认解决了。
