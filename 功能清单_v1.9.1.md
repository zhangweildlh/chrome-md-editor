# Chrome-Markdown-Edit 功能清单（v1.9.1）

> 本清单基于全量源码阅读（`src/editor.js`、`src/compare.js`、全部 71 个 `src/**/*.js` 模块、`editor.html`、`compare.html`）整理。
> 覆盖主界面（编辑器 + 预览）与对比/合并页全部功能，并对每项标注：**用途 / 使用场景 / UI 入口（按钮 · 快捷键 · 唤起方式）**。
> 标注说明：✅ 有对应 UI 按钮；⌨️ 有快捷键；🔧 自动/隐式触发；🧩 代码模块提供（无独立按钮，由其它入口调用）。

---

## 一、文件操作

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 打开文件 | 用系统选择器打开 `.md`/`.txt`（优先 File System Access API 以保留写回句柄） | 编辑本地 Markdown | ✅ `打开` 按钮（btnOpen）· ⌨️ `Ctrl/⌘+O` |
| 保存文件 | 将当前内容写回原文件或另存为 | 落盘成果 | ✅ `保存` 按钮（btnSave）· ⌨️ `Ctrl/⌘+S` |
| 新建 | 清空为未命名空文档，键归并到 `unsaved` 避免串档 | 开新稿 | ✅ `新建` 按钮（btnNew） |
| 打开文件夹 | 用 FSAPI 挂载目录，侧栏浏览项目、稳定预览相对路径图片 | 多文件项目 / 相对路径图片 | ✅ `打开文件夹`（btnOpenFolder）· 侧栏 `刷新`（btnRefreshTree）· `收起`（btnCollapseSidebar） |
| 拖入文件 | 把 `.md` 拖进窗口直接打开 | 快速打开 | 🔧 拖放（`onboarding` 引导） |
| 自动保存草稿 | 编辑停顿 800ms 后把正文写入 `chrome.storage.local`（`draft::键`），**不碰磁盘原文件** | 防意外丢失未保存内容 | 🔧 自动（关闭/重开时弹 `offerDraftRestore` 提示恢复） |
| 周期快照环 | 每 2 分钟或每 50 次变更压入一份快照，最多 30 份（`snapshots::键`） | 回退到历史版本 | ✅ `快照` 按钮（btnSnapshots）打开快照面板，可点选恢复 |
| 定时落盘副本 | 用户显式开启后，每 N 秒（默认 30）在源文件同目录生成 `文件名_时间戳.md`，**只建新副本、绝不覆盖源文件** | 防误改 / 留痕 | ✅ `自动保存` 按钮（btnAutosaveDisk）+ 旁边秒数输入框（5–3600 秒） |

---

## 二、编辑与格式化（工具栏）

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 加粗 | 包裹 `**...**` | 强调 | ✅ `B`（btnBold）· ⌨️ `Ctrl/⌘+B` |
| 斜体 | 包裹 `*...*` | 轻强调 | ✅ `I`（btnItalic）· ⌨️ `Ctrl/⌘+I` |
| 删除线 | 包裹 `~~...~~` | 废弃内容 | ✅ `S`（btnStrike） |
| 行内代码 | 包裹 `` `...` `` | 代码/标识符 | ✅ `</>`（btnCode） |
| 居中 | 行首加居中标记 | 标题居中 | ✅ `居中`（btnStyleCenter） |
| 高亮 | 写入 `<mark>`（源码 `==...==`）并实时渲染，再点取消 | 重点标注 | ✅ `A`（btnStyleHighlight）· 预览区右键「高亮」 |
| 颜色 | 给选中文字上色（调色板取色） | 彩色强调 | ✅ `颜色`（btnColor）+ 调色板 |
| 字号 | 给选中文字设字号（弹层选档） | 局部字号 | ✅ `字号`（btnFontSize）+ 弹层 |
| 标题 H1/H2/H3 | 行首插入 `# ` / `## ` / `### ` | 分级标题 | ✅ `H1/H2/H3`（btnH1/2/3） |
| 无序列表 | `- ` 列表 | 要点 | ✅ `•`（btnUL） |
| 有序列表 | `1. ` 列表 | 步骤 | ✅ `1.`（btnOL） |
| 引用 | `> ` 引用块 | 引述 | ✅ `❝`（btnQuote） |
| 代码块 | 插入 ```` ``` ```` 围栏 | 代码段 | ✅ `代码块`（btnCodeBlock） |
| 链接 | 插入 `[文字](url)` | 超链接 | ✅ `链接`（btnLink） |
| 表格 | 插入 Markdown 表格模板 | 结构化数据 | ✅ `表格`（btnTable） |
| 水平线 | 插入 `---` | 分隔 | ✅ `横线`（btnHR） |
| 图片 | 插入 `![alt](src)`（支持粘贴/相对路径） | 配图 | ✅ `图片`（btnImage）；编辑区粘贴截图自动写 `images/` 或嵌入 data URL |

---

## 三、查找 / 替换 / 工作区搜索

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 查找 / 替换 | 官方 CM6 搜索语义（命中高亮、正则、整词、大小写）+ 实时 `X/Y` 命中计数 + 全选匹配（多光标） | 定位与批量改 | ✅ `查找`（btnFind）· ⌨️ `Ctrl/⌘+F`；面板内 `↑/↓` 跳、`Enter` 下一个、`Shift+Enter` 上一个、`Esc` 关闭 |
| 全选匹配 | 把所有命中变多光标同时编辑 | 批量改同名 | ✅ 面板 `全选` 按钮 |
| 工作区搜索 | 在当前已打开文件夹内**所有** `.md` 中检索关键词，列出「文件:行号 + 片段」 | 跨文件定位 | ✅ 查找面板内 `工作区搜索` 子按钮（v1.8.9 起由独立入口并入）；点击结果在该文件对应行打开 |
| 工作区替换 | 当前文档走编辑器替换，其余文件**直接落盘**（不可撤销，二次 `confirm` 确认） | 跨文件改名/替换 | ✅ 面板 `全部` 按钮（工作区模式） |

---

## 四、预览与所见即所得（WYSIWYG）

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 分屏 / 仅编辑 / 仅预览 | 切换三态布局 | 不同写作偏好 | ✅ `视图` 分段按钮（btnViewSplit/Edit/Preview） |
| 预览实时渲染 | 右侧 markdown-it 渲染，支持标题/列表/表格/代码高亮/引用等 | 边写边看 | 🔧 自动（编辑器变更触发 `updatePreview`） |
| 预览区直接编辑（WYSIWYG） | 预览 `contenteditable`，输入 `**测试一**`、`[文字](url)` 实时渲染为粗体/链接，并**同步回写**左侧源码对应位置（往返渲染 + `html-to-markdown` 还原） | 所见即所得改稿 | ✅ 直接在预览区点改；`isPreviewEditing` 守卫防回环 |
| GitHub 风格 Callout | `> [!NOTE]`/`[!WARNING]` 等渲染为带标题图标提示块（16 种类型，含中文名） | 提示框 | 🧩 语法驱动（`callout.js`）；`data-callout` 保证回写还原 |
| `==高亮==` | 行内 `==文本==` 渲染为 `<mark>`（可逆 DOM，回写还原 `==`） | 行内重点 | 🧩 语法（`highlight-plugin.js`）+ 工具栏 `A` 上色 |
| Mermaid 图 | ```` ```mermaid ```` 代码块渲染流程图/时序图 | 画架构/流程 | 🧩 语法；每个图带 `⛶` 全屏按钮 |
| Mermaid 全屏缩放/平移 | 全屏浮层 + `Ctrl/⌘+滚轮` 以光标为中心缩放 + 拖拽平移 + `+/-/重置/关闭` | 看大图细节 | ✅ 预览图右上 `⛶` 按钮；`Esc`/点背景关闭 |
| 本地图片解析 | 相对路径/绝对路径/data URL/blob/file 等多源解析为可显示 `src`；粘贴图命名 `pasted-时间戳-随机.png` | 配图预览 | 🔧 自动（`image-support.js`） |
| 链接安全跳转 | 仅放行 `http/https/mailto/file` 协议，预览点击在合法范围打开 | 防恶意协议 | 🔧 自动（`link-support.js`） |
| 预览 Base64 折叠 | 超长 `data:` 图片行折叠为占位，点击展开 | 避免长行卡顿 | 🔧 自动（仅 `>200` 字符）；点击占位展开 |

---

## 五、斜杠菜单（Slash Menu）

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 命令面板 | 行尾输入 `/` 唤起命令（标题/粗体/列表/代码块/引用/表格/分割线/图片/链接等），`↑↓` 选择、`Enter/Tab` 执行、`Esc` 关闭 | 不用鼠标快速插入结构 | 🔧 输入 `/`（中文顿号 `、` 已取消触发）；代码块内不触发；无匹配命令时自动关闭不吞键 |

---

## 六、块拖拽（Block Drag）

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 块拖拽排序 | 每个块首行左侧出现拖拽手柄，拖动调整块顺序；列表项支持横向深度（缩进层级） | 重排段落/列表 | 🔧 每块首行左侧手柄（HTML5 drag + pointer 兜底）；带虚线落点指示与残影 |
| 选区拖拽 | 选中多行后首行出现拖拽手柄，整段（吸附到块边界）搬运 | 移动大段内容 | 🔧 选中后左侧 `拖拽选中区域` 手柄 |
| 下方插入块 | 块首行 `+` 按钮在当前块下插入空块 | 快速加段 | ✅ 块首行 `+` 按钮 |

---

## 七、大纲与任务面板

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 大纲面板 | 遍历语法树收集 H1–H6，生成可点击层级列表，点击跳转到对应标题 | 长文导航 | ✅ `大纲`（btnOutline）打开左侧面板 |
| 任务清单 | 解析 `- [ ]`/`- [x]`（含 `*`/`+`/有序），可勾选，状态回写源码 `[ ]`/`[x]` | 待办管理 | ✅ `任务`（btnTasks）打开面板；勾选框直接切换 |

---

## 八、视图模式 / 专注 / 打字机 / 显示设置

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 视图模式四态 | `日常 / 专注 / 沉浸 / 全显` 循环切换外壳元素显隐（DOM class 切换，不重创编辑器） | 不同专注度 | ✅ `⊞`（btnChromeMode）循环；状态存 `localStorage` |
| 专注模式 | 淡化非当前行（`.focus-mode`） | 纯写作 | ✅ `专注`（btnFocusMode） |
| 打字机模式 | 光标行始终居中 | 沉浸式输入 | ✅ `打字机`（btnTypewriter） |
| 显示设置 | 编辑器/预览独立字号 + 界面密度（紧凑/标准/舒适），写入 CSS 变量 | 个性化排版 | ✅ `显示`（btnDisplaySettings）弹层 |

---

## 九、翻译（双语对照）

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 预览双语翻译 | 扫描预览块级单元（标题/段落/列表/表单元格），调用翻译 API，在原文下注入译文 span；支持 Anthropic/OpenAI/DeepL 兼容与多预设；内存缓存避免重复请求 | 读英文资料即时中译 | ✅ `翻译`（btnTranslate）打开设置+执行；设置含 API Key/端点/模型/目标语言，走后台代理规避 CORS |
| 翻译设置 | 预设（MiniMax/DeepL/OpenRouter 等）、自定义端点、目标语言 | 配置翻译源 | ✅ 翻译面板内设置项；持久化 `chrome.storage.local` |

---

## 十、主题与语法配色

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 编辑器主题 | 23 套配色（含豆沙绿亮/暗），`data-theme`×`data-editor-theme`×`data-color-scheme`×`data-skin` 四维度正交 | 个性化外观 | ✅ `主题` 下拉（btnTheme + editorThemeSelect） |
| 语法高亮配色 | 3 套配色方案：经典 / 护眼（米黄）/ 高对比（`data-color-scheme`），与深浅主题正交 | 代码块配色偏好 | 🧩 经主题/显示设置联动（`md-theme-tokens.js`）；持久化 |

---

## 十一、会话与状态恢复

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 上次文件恢复 | 记住正文+文件名（不记句柄，30 天有效），重开尽量恢复 | 续写 | 🔧 自动（`session-restore.js`）；受 Chrome 限制保存时可能重选路径 |
| 滚动位置恢复 | 预览 `innerHTML` 重建 / 编辑器全量替换后恢复 `scrollTop`（rAF 兜底） | 渲染不跳顶 | 🔧 自动（`scroll-restore.js`） |
| 新手引导 | 空白时展示开始卡片；也可随时看使用说明与示例说明书 | 上手/查用法 | ✅ `?`（btnHelp）打开；含「拖入/打开文件夹/示例说明书」入口 |
| 反馈入口 | 状态栏 `反馈` 链接跳转 GitHub Issues | 报 bug/建议 | ✅ 状态栏右侧 `反馈` 链接（feedback.js） |

---

## 十二、对比 / 合并页（compare.html）

> 进入：主界面 `对比合并`（btnCompare）。以下为对比页功能区。

| 功能 | 用途 | 使用场景 | UI 入口 |
|------|------|----------|---------|
| 两栏 / 三栏 | `two`：Yours↔Theirs 对照；`three`：Yours(只读) / Result(可编辑) / Theirs(可编辑) 三方合并 | 双版对照 / 三方合并 | ✅ `两栏`/`三栏`（btnViewTwo/Three） |
| 选择文件 | 多选/拖拽载入对比文件，单选取当前激活栏 | 载入对比对象 | ✅ `选择文件`（btnPickFiles）· 拖拽区 |
| 块导航 | 在差异块间跳转（边界正确步进 + 环绕） | 逐个看差异 | ✅ `上一块`/`下一块`（btnPrevChunk/NextChunk）· ⌨️ `Shift+B`/`[` 上、`B`/`]` 下；编辑区内用 `Alt+B`/`Alt+Shift+B` |
| 概览（Location Pane） | 概览条展示双层移动/差异分布，点击跳转 | 全局把握差异 | ✅ `概览`（btnToggleLocationPane，键 `L`） |
| 折叠未改 | 折叠无差异区域（margin 3 / minSize 6），两文件完全相同时不折叠 | 聚焦改动 | ✅ `折叠未改`（btnToggleCollapse，键 `E`） |
| 行内字词差异 | 差异块内字词/字符级高亮（off/word/char 切换，含 N↔M 行数不等块的同源配对） | 看清具体改动 | ✅ `行内高亮` 下拉（selHighlightWords） |
| 块移动连线 | 移动过的块以蓝色标识 + SVG 跨栏连线（A↔B / B↔C 双层） | 识别块搬家 | 🔧 自动（`move-detection.js` + `move-connectors.js`） |
| 接受此块 | 块级按钮 `⇄ 采纳此块`（Yours→Result）；冲突块显双向 `采纳左 ▶`/`◀ 采纳右` | 逐块合并 | ✅ 每块左侧 `cm-compare-revert` 按钮组 |
| 接受对方块 | 把光标所在 Theirs 块拷入 Result | 快速采纳 | ✅ `接受对方块`（btnAcceptTheirs） |
| 应用非冲突 | 一次性应用所有非冲突改动 | 快速合并 | ✅ `应用非冲突`（btnApplyNonConflicting） |
| 批量方向 | 全部采纳左 / 全部 / 全部采纳右 | 整体取向 | ✅ `采纳左`/`全部`/`采纳右`（btnAcceptLeft/All/Right） |
| 保存结果 | 把当前栏写回源文件（FSAPI 句柄/Tauri 路径原地写，否则另存为） | 落盘合并结果 | ✅ `保存`（btnSave）· ⌨️ `Ctrl/⌘+S` |
| 导出结果 | 把合并结果导出为文件 | 交付 | ✅ `导出结果`（btnExportResult） |
| 导出 Diff | 导出差异报告（HTML/文本） | 留痕/评审 | ✅ `导出Diff`（btnExportDiff） |
| 图片对比 | 对比页插入/对照图片 | 图文对照 | ✅ `图片`（btnAddImages） |
| 应用 / 放弃 | 确认采用结果 / 放弃合并回主界面 | 收尾 | ✅ `应用`（btnApplyMerge）/`放弃`（btnAbortMerge） |
| 返回主界面 | 从对比页回到编辑器（本次 v1.9.1 新增，带 100ms 复位避免误触跳转） | 退出对比 | ✅ `返回主界面`（btnBackToEditor，本次新增） |

---

## 十三、对比页支撑子模块（🧩 由上述 UI 调用，无独立按钮）

- `compare-merge.js`：封装 `@codemirror/merge` 的 `MergeView`，两栏/三栏 diff、装饰调度、冲突判定、连线图层、滚动联动。
- `compare-nav.js`：块导航纯实现（`bindChunkNavigation` / 键位解析），避免库内 `goToNextChunk` 在 MergeView 包裹层失效。
- `compare-line-markers.js`：差异块红绿行号标记。
- `compare/location-pane.js`：概览条数据 + 跳转。
- `compare/images.js`：图片对比载入。
- `compare/diff-export.js`：差异报告导出。
- `compare/inline-word-diff.js`：行内字词级 diff 计算（word/char 模式）。
- `compare/delta-align.js`：移植自 delta 的 N↔M 块同源行配对 + 独属行清单。
- `compare/move-detection.js` + `move-decorations.js` + `move-connectors.js`：块移动检测、蓝色装饰、SVG 跨栏连线。
- `compare/chunk-ops.js`：块级「采纳」统一写入原语（区间校验、单次 dispatch）。
- `compare/io-bridge.js` + `compare/save.js`：Ctrl+S 回写（FSAPI 句柄 / Tauri 路径 / 另存为降级）。
- `compare/shims.js` + `desktop-shims.js`：360Chromex / Tauri 双端环境垫片（File Picker、存储、运行时差异）。

---

## 十四、编辑器内核基础设施（🧩 隐式提供编辑体验）

- **括号/符号配对**：`closeBrackets`（编辑器侧自动补闭括号）+ `auto-pair.js`（预览区输入开符号自动补闭符号，对齐 CM6 行为，含中英文/全角引号）。
- **括号匹配高亮**：`bracketMatching` + `bracket-utils.js` / `close-brackets-config.js` 配置。
- **代码块语言补全**：```` ``` ```` 后语言名候选（`codeblock-complete.js`，`py→python` 等简写展开，模糊匹配）。
- **历史/撤销**：`history` + `historyKeymap`（`Ctrl/⌘+Z` 撤销、`Ctrl/⌘+Shift+Z` 重做）。
- **选区高亮 / 搜索高亮**：`highlightSelectionMatches` + 自定义 `md-editor-highlight.js` / `md-preview-highlight.js`。
- **语法高亮**：`@codemirror/lang-markdown` + `@codemirror/language-data`（100+ 语言）。
- **行号 / 活动行高亮 / 矩形选择 / 跨光标**：CodeMirror 基础扩展（`lineNumbers` / `highlightActiveLine` / `rectangularSelection` / `crosshairCursor`）。
- **工具栏滚动**：`toolbar-scroll.js`（窄屏工具栏横向滚动）。
- **多端单 EditorView**：浏览器 MV3 与 Tauri(EXE) 共用同一 `src/editor.js` 核心，靠 shims 适配。

---

## 小结

- **主界面功能**约 60+ 项，绝大多数有对应工具栏按钮或快捷键；仅「草稿自动保存 / 会话恢复 / 滚动恢复 / 预览实时渲染 / Callout / `==高亮==` / 块移动连线 / 符号自动配对」等为语法/隐式触发。
- **对比/合并页**为独立功能区，提供两栏/三栏 diff、块导航、行内字词差异、块移动连线、逐块/批量采纳、导出结果/差异、返回主界面等完整合并工作流。
- **v1.9.1 本次新增/修复相关**：对比页 `返回主界面` 按钮（`btnBackToEditor`，带 100ms 复位）；7 项预览/对比 BUG 修复（含预览 WYSIWYG 双向同步已验证无需改代码）。

> 说明：本清单逐项均经源码核实（模块导入、事件绑定、`editor.html`/`compare.html` 按钮 ID 与快捷键 title）。如需某功能的更深层实现说明（如 diff 调度器的 rAF 轮询、快照环去重、翻译 API 路由），可进一步展开。
