# 功能需求实施方案（讨论结论）

- **文档性质**：需求讨论敲定结论 + 实施落点梳理（非代码实现）
- **生成日期**：2026-08-17
- **适用仓库**：`chrome-md-editor`（fork：`zhangweildlh/chrome-md-editor`）
- **目标版本**：待定（建议 `v1.9.2` 或 `v1.10.0`，实施时与 `package.json` / `public/manifest.json` 同步）
- **当前状态**：需求层全部敲定，**待用户二次显式授权后**方可改动源码与文件
- **关联文档**：`功能清单_v1.9.1.md`（既有功能清单）

---

## 一、总览与提交批次

本次共 **10 项**需求：原始 8 项 + 新增 A（编辑栏多处选中闪烁）+ 新增 B（预览栏 Markdown 实时编辑）。

按"分两批提交"的既定策略：

| 批次 | 范围 | 包含需求 |
|---|---|---|
| **第一批（编辑/预览页）** | 主界面编辑器与预览器 | 1、2、3（主界面部分）、4、5、6、A、B（确认现状） |
| **第二批（对比/合并页）** | 对比/合并视图 | 7、8、3（对比页 Ctrl+O 部分）、A（对比页继承，随扩展集自动生效） |

> 需求 A 的修复落在共享扩展 `editor-extensions.js`，主界面与对比页共用 `createEditorExtensions`，故修复一次即双页生效；第二批仍建议二次确认对比页 CM 实例确实走该扩展集。

---

## 二、逐条需求明细

### 需求 1：采纳"编辑区 8 套 + 预览区 8 套"高亮方案

- **原始诉求**：采纳先前推荐的编辑区 8 套、预览区 8 套 Markdown 高亮/代码着色方案。
- **已敲定决策**：方案层采纳。地基已具备——`src/md-editor-highlight.js`（编辑区 markdown tags）与 `src/md-preview-highlight.js`（预览区 hljs tokens）均已映射到 CSS 变量、零硬编码颜色，天然支持"命名调色板"切换。
- **代码落点**：`src/md-editor-highlight.js`、`src/md-preview-highlight.js`（CSS 变量映射层）。
- **纳入修改**：
  1. 固化"8+8"具体命名清单（沿用首条消息所引推荐，实施前从推荐方案与 `功能清单_v1.9.1.md` 对齐确认）；
  2. 将命名方案接入主题/方案选择机制（见需求 2 的切换入口）。
- **状态**：方案层已采纳；8+8 具体命名待实施固化。

### 需求 2：主界面新增"高亮方案选择"按钮（编辑区/预览区解耦，且与主题解耦）

- **原始诉求**：主界面新增按钮，弹出菜单分别选择"编辑区 Markdown 语法高亮方案"与"预览区 Markdown 渲染后代码着色方案"；编辑区与预览区方案互解耦，且与主题颜色方案解耦。
- **已敲定决策**：在主界面工具栏新增按钮 + 弹出菜单；两个方案各用独立 `localStorage` 键持久化；分别写入独立 DOM 属性 `data-md-syntax-scheme`（编辑区）与 `data-code-scheme`（预览区），**不触碰** `data-theme` / `data-editor-theme`（主题颜色方案的单一事实源）。
- **代码落点**：
  - `src/editor.html` 工具栏（参考 `appearance-group` 517-532、`view-switch-group` 539-542 旁新增按钮 + 弹出菜单）；
  - `src/editor.js` 读取/写入两个独立 scheme 并应用到对应 DOM 属性；
  - `src/editor.css` / 主题预设（`src/theme-presets.js`）确保 scheme 切换只改高亮变量、不改主题色。
- **纳入修改**：
  1. `editor.html` 新增按钮与弹出菜单 UI；
  2. `editor.js` 实现双 scheme 读写（独立于主题预设的 `applyEditorThemePreset`）；
  3. 持久化键独立，避免与主题键冲突；
  4. 验证主题切换不覆盖、不被覆盖方案选择。
- **状态**：方案敲定，待实施。

### 需求 3：所有界面"打开文件"(Ctrl+O) 支持 TXT 打开载入

- **原始诉求**：所有界面的"打开文件"按钮 (Ctrl+O) 均需支持 TXT 文件的打开、载入；对比页也要加 Ctrl+O 快捷键。
- **已敲定决策**：主界面与对比页均支持 TXT 载入；对比页补 `Ctrl+O` 快捷键。
- **代码落点——主界面**：
  - `src/file-picker.js`：`MD_OPEN_TYPES`（6-9）增 `.txt`；`openFileViaPicker`（22 / 38）accept 增 `text/plain,.txt`；`MD_SAVE_TYPES`（11-14）保持 `.md`（载入支持即可，保存可保持原类型）；
  - `src/editor.js`：`Mod-o`（385）→ `handleOpen`（1673）→ `openFileViaPicker` 链路已通。
- **代码落点——对比页**：
  - `src/compare-files.js`：`DEFAULT_ACCEPT`（25）**已含** `.txt`，`pickFiles` / `pickSingleFile` 已支持 txt；
  - `src/compare.js`：当前**无** `Ctrl+O` keymap → 需新增 `Mod-o` 绑定到打开逻辑。
- **纳入修改**：
  1. 主界面 `file-picker.js` 接受 `.txt` 扩展名；
  2. 对比页 `compare.js` 新增 `Ctrl+O` 快捷键。
- **状态**：方案敲定，待实施。

### 需求 4：MD/TXT 扁平"聚集"列表 ↔ 完整嵌套树 切换；depth 上限 5→8

- **原始诉求**：文件浏览器栏"打开文件夹""刷新"之后、"收起侧边栏"之前新增按钮，实现 MD/TXT 扁平"聚集"列表与完整嵌套目录树的切换（扁平列表仅显示 MD/TXT）；递归深度上限 `depth < 5` 改为 `depth < 8`。
- **已敲定决策**：插入切换按钮；实现"扁平聚集"渲染模式（仅 MD/TXT，扁平列出）；深度上限放宽到 8。
- **代码落点**：
  - `src/editor.html` `sidebar-actions`（645-663）：在 `btnRefreshTree`（652）与 `btnCollapseSidebar`（658）之间插入新按钮；
  - `src/editor.js` `readDirectoryRecursive(dirHandle, depth=0, parentPath)`（3394）：`depth < 5`（3403）改为 `depth < 8`；
  - `src/editor.js` `renderFileNode`（3514）：`isMarkdown`（3519）正则 `/\.(md|markdown|mdown|mkd|mkdn|txt)$/i/` **已含** txt；
  - 新增"扁平聚集"渲染分支（仅 MD/TXT 扁平列表）。
- **纳入修改**：
  1. 插入切换按钮；
  2. 深度上限 `5 → 8`；
  3. 实现扁平聚集列表渲染（新函数或模式分支）。
- **状态**：方案敲定，待实施。

### 需求 5：滚动同步按钮以对比页为基对齐，文案改"同步"

- **原始诉求**：编辑/预览页"滚动同步"按钮图标与文案以对比/合并页为基对齐，文案"滚动同步"改为"同步"。
- **已敲定决策**：文案统一为"同步"；图标/样式对齐对比页（纯图标 + title 提示）。
- **代码落点**：
  - `src/editor.html` `btnScroll`（368）：文字"滚动同步"→"同步"；
  - `src/compare.html` `btnScroll`（89-90）：作为对齐基准（纯图标 + title）。
- **纳入修改**：`editor.html:368` 文案改"同步"，图标样式对齐对比页。
- **状态**：方案敲定，待实施。

### 需求 6：消除 gutter 与拖拽手柄间空白；收窄手柄区 20%

- **原始诉求**：编辑/预览页编辑栏中，快折叠/展开区与拖拽块(手柄)区之间有空白（经截图像素级分析，实为内容区最左侧 **gutter 与 markra 块拖拽手柄之间的 0.4em 预留槽位**），取消此空白；收窄拖拽块(手柄)区 20%。
- **已敲定决策**：取消该预留槽位空白；收窄手柄区 20%；**同意把 `.cm-content` 提升到 `z-index:201`** 以消除 gutter 与手柄的层叠间隙。
- **代码落点**：
  - `src/editor.css` `.cm-content { padding: 16px 20px 16px 1.8em }`（1534）：减小左内边距以消除 gutter 与手柄间隙；`.cm-content` 提升 `z-index:201`；
  - `src/block-drag.js` `blockDragTheme`（970-978）：`.cm-markra-block-toolbar { marginInlineStart: "-1.4em" }`（974）与 `.markra-block-drag-dot`（992-999，宽高 `0.18em`）按 20% 收窄。
- **纳入修改**：
  1. 调整 `.cm-content` 左内边距 + `z-index:201`；
  2. block-drag 手柄尺寸/偏移收窄 20%（注意 `editor.css:1528-1547` 注释：过度压窄会让 `.cm-markra-block-toolbar` 被 sticky gutter 覆盖失效，需实测下限）。
- **状态**：方案敲定，待实施。

### 需求 7：对比/合并页新设"大纲面板"按钮 + 随激活窗口实时显示

- **原始诉求**：对比/合并页新设"大纲面板"按钮显示/隐藏大纲窗口，式样对齐编辑/预览页；大纲随鼠标激活窗口栏实时显示（激活左栏显文件一，激活右栏显文件三，以此类推）。
- **已敲定决策**：从当前 `#locationPane` 中把"大纲"提升为**独立面板**；新增"大纲面板"按钮（式样对齐编辑/预览页 `#outlinePanel`）；大纲源随鼠标激活的 MergeView pane 实时切换。
- **代码落点**：
  - `src/compare.html`：新增"大纲面板"按钮（参考 `src/editor.html` `#outlinePanel` 729 式样）；
  - `src/compare/location-pane.js`：`getOutlineItems`（59，来自 outline.js）、`.lp-section-outline`（432-450，当前挂于 `#locationPane` 内）→ 提升为独立 `#outlinePanel`；
  - `src/compare.css` `.lp-section-outline`（751）。
- **纳入修改**：
  1. 新增大纲面板按钮 + 独立面板（式样对齐编辑页）；
  2. 实现"激活窗口实时切换大纲源"：监听 MergeView 各 pane 的鼠标激活，左栏激活→文件一大纲，右栏激活→文件三大纲（按 `getOutlineItems` 源切换）。
- **状态**：方案敲定，待实施。

### 需求 8：差异概览与大纲分离；差异概览退化为最右侧可点击粗线

- **原始诉求**：对比/合并页"差异概览"与"大纲窗口"分离；差异概览放最右侧、收窄为一条粗线宽度（鼠标能准确点中即可）。
- **已敲定决策**：采纳**解读 A**——差异概览退化为最右侧可点击粗线，仅做差异导航（下一/上一差异跳转），无缩略图详情；`btnToggleLocationPane` 文案"概览"→"差异"。
- **代码落点**：
  - `src/compare.html` `#btnToggleLocationPane`（35）：文案"概览"→"差异"；`#locationPane`（166）拆分；
  - `src/compare.css` `.compare-location-pane`（679）重构为细线导航。
- **纳入修改**：
  1. 按钮文案改"差异"；
  2. `#locationPane` 拆除大纲后，差异部分退化为最右侧可点击粗线（点击跳转差异）；
  3. 移除缩略图详情（若存在）。
- **状态**：方案敲定，待实施。

### 需求 A：编辑栏多处选中字符串闪烁（highlightSelectionMatches）

- **原始诉求**：编辑栏中鼠标选中字符串，若该串在全文出现多处，会出现显示闪烁，需消除；同时核查对比/合并页是否存在相同问题，若存在一并消除。
- **已敲定决策（本次确认）**：**保留功能 + 消除闪烁**；**接受"拖动中不高亮、松开后一次性高亮全部"**。
- **根因定位（已核查）**：
  - 罪魁 = `src/editor-extensions.js:87` 的 `highlightSelectionMatches()`（无参数默认 `highlightSelection:true`、`maxMatches:100`、装饰类 `cm-selectionMatch`）。
  - 机制：它只装饰"选中范围之外"的相同串。全文仅一处时零额外装饰→不闪；多处时生成多个 `<span class="cm-selectionMatch">`，拖动选中过程中选区文本逐帧变化→匹配集逐帧重建重绘→闪烁；选区落定（文本固定）后匹配集稳定→不闪。
  - **已排除 CSS 过渡重放**：`src/editor.css` 所有 `transition` 均落在 UI 元素（`.resizer`/`.tree-item`/`.sidebar-toggle`/`.workspace-search-result`/`body` 主题切换），**无一命中** `.cm-selectionMatch` 或编辑器文本；src 内对 `cm-selectionMatch` 无自定义样式。
  - 反向佐证：若闪烁来自 markra 选区拖拽手柄，则一处时也会闪（手柄对任意选区都出现），但一处不闪→排除手柄，锁定 `highlightSelectionMatches` 多处装饰。
  - 对比页：因 `src/compare.js:33` `import { createEditorExtensions }` 复用同一扩展集，同样存在；修复 `editor-extensions.js` 即自动覆盖。
- **代码落点**：`src/editor-extensions.js:87`（替换内置调用）、`src/editor.css`（防御性样式）。
- **纳入修改**：
  1. 自写 ViewPlugin 替换内置 `highlightSelectionMatches()`，实现"**选区落定后才重算**"（连续 `selectionSet` 期间跳过，仅在稳定后 debounce ~150ms 或末态一次性计算）；
  2. 仅扫视口 `view.visibleRanges` + `maxMatches` 封顶，避免大文档全量扫描拖帧；
  3. 选中文本与上次查询相同则跳过重算（幂等备忘，避免几何变化触发 churn）；
  4. 防御性补 `.cm-selectionMatch { transition: none !important; }`。
- **状态**：方案敲定，待实施。

### 需求 B：预览栏输入 Markdown 实时渲染 + 反向同步

- **原始诉求**：核查并（若未实现则）实现——用户直接在预览栏输入 Markdown 语法（`**` `~~` `#` `<mark>` `<center>` 等），预览栏实时无闪烁渲染，并同步在编辑栏展示/添加对应原始语法。
- **核查结论（重要更正）**：**该功能已 substantial 实现**，并非"未实现"。当前 `previewContainer` 已是 `contenteditable`，并具备：
  - `src/editor.js` `renderLivePreviewMarkdown()`（1529）：150ms 防抖实时渲染（往返 `htmlToMarkdown→md.render`）；
  - `src/editor.js` `syncPreviewToEditor()`（1333）：500ms 防抖反向同步到编辑栏（`setEditorContent`）；
  - `src/editor.js` input/blur 监听（1202-1233）、符号自动配对、选区记忆、右键高亮菜单。
- **已敲定决策**：
  - **表格/代码/mermaid 维持"保护不可编辑"**（`protectPreviewBlocks`，editor.js:1412-1433，置 `contenteditable=false` 防数据损坏）——用户已确认此策略。
  - 语法覆盖已为全集：渲染层 `md.render` 全支持；含富 HTML 的简单标签（`<center>`/`<font>`/`<mark>`/`<span>` 等）经 `html:true` 透传 + DOMPurify 放行（editor.js:209）+ `html-to-markdown.js` `reconstructRawTag`（256-261）/ `==` 可逆（252-255）往返保真，反而是 R1 强项；结构化块（表格/代码/mermaid/数学）维持保护。
  - **当前编辑栏与预览栏编辑均"无闪烁"**（用户确认）——故需求 B 的预览渲染路径（虽用 `innerHTML` 整树替换，editor.js:1559）本次**不做强制改动**。
- **代码落点**：`src/editor.js:1202-1233`、`1333`、`1529`、`1412-1433`；`src/editor.js:192-247`（markdown-it `html:true` + 插件）；`src/html-to-markdown.js:216`（表格）、`252-261`（mark/center/font/span）。
- **纳入修改（最小）**：
  1. 确认保持现状（已实现且工作正常）；
  2. 固化决策"表格/代码/mermaid 维持保护不可编辑"；
  3. （**可选/观测项，非强制**）若未来预览栏出现闪烁，再将 `editor.js:1559` 的 `innerHTML` 替换换成 morphdom / incremental-dom 增量 patch——本次不做。
- **状态**：已实现，本批无强制代码改动，仅固化决策。

---

## 三、两批提交计划（实施时）

### 第一批（编辑/预览页）
- 需求 1（8+8 方案固化）
- 需求 2（高亮方案选择按钮 + 双 scheme 解耦）
- 需求 3（主界面 TXT 载入）
- 需求 4（扁平聚集列表切换 + depth<8）
- 需求 5（滚动同步文案/图标对齐）
- 需求 6（gutter/手柄空白消除 + 手柄收窄 20%）
- 需求 A（highlightSelectionMatches 落定式重算 + 防御样式）
- 需求 B（确认现状，无强制改动）

### 第二批（对比/合并页）
- 需求 7（大纲面板按钮 + 独立面板 + 激活窗口实时切换）
- 需求 8（差异概览退化右侧粗线 + 按钮文案"差异"）
- 需求 3（对比页 `Ctrl+O` 快捷键）
- 需求 A（对比页继承——随 `editor-extensions.js` 自动生效，实施时二次确认 `compare.js` 走 `createEditorExtensions`）

---

## 四、执行与提交纪律（本项目强制红线）

1. **路径核验先行**：任何 git/gh 操作前确认 `.git` 与仓库目录。
2. **禁止强推 / 删除** `origin/main` 等受保护分支；破坏性动作（强推/删分支/打 tag/发版）一律暂停并三段式二次授权。
3. **只推自有 fork(origin)** `zhangweildlh/chrome-md-editor`，**绝不推 upstream** `yishu-ziyu/chrome-md-editor`；PR 走 fork main。
4. **两批分开** commit / PR，互不混入。
5. **隐私闸门**：提交前 `--dry-run` 核对，防 `.test-run/` 真实浏览器 profile 等隐私泄漏入仓。
6. 版本戳六处同步点（升版本时）：`package.json` + `public/manifest.json` + `src/editor.js` + `src/editor.html` + `src/compare.js` + `src/compare.html`。

---

## 五、待二次授权后实施清单（汇总）

| 需求 | 主要代码改动文件 | 关键改动点 |
|---|---|---|
| 1 | md-editor-highlight.js / md-preview-highlight.js | 固化 8+8 命名并接入切换 |
| 2 | editor.html / editor.js | 新增按钮+菜单；双 `data-*` 属性 + 独立 localStorage |
| 3 | file-picker.js（主界面）、compare.js（对比页） | 接受 .txt；对比页补 Ctrl+O |
| 4 | editor.html、editor.js | 插入切换按钮；depth<8；扁平聚集渲染 |
| 5 | editor.html | 文案"同步" + 图标对齐对比页 |
| 6 | editor.css、block-drag.js | `.cm-content` 左内边距 + z-index:201；手柄收窄 20% |
| 7 | compare.html、compare/location-pane.js、compare.css | 大纲独立面板 + 激活窗口实时切换 |
| 8 | compare.html、compare.css | 差异退化右侧粗线；按钮"差异" |
| A | editor-extensions.js、editor.css | 自写落定式 ViewPlugin + 防御样式 |
| B | （无强制改动） | 确认现状；固化"结构化块保护"决策 |

> 本文件为讨论结论归档，所有源码改动须待用户二次显式授权后实施。
